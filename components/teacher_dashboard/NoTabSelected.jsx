const NoTabSelected = ({ className }) => {
  return (
    <h1 className={`${className} text-center text-primaryColor font-semibold`}>
      Selecciona una opción en la barra superior
    </h1>
  )
}

export default NoTabSelected